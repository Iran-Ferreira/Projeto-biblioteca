package com.biblioteca.model;

public final class Administrador extends Usuario {

    public Administrador(String nome) {
        super(nome);
        // No futuro, poderia ter atributos como "nivelDeAcesso"
    }
}